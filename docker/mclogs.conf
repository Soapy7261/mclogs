server {
    listen 80;
    sendfile off;

    server_name paste.cloudykingdom.com;

    root /web/mclogs/web/public;

    error_log /var/log/nginx/mclogs-error.log error;

    client_max_body_size 210m;

    location / {
        try_files $uri /index.php;
    }

    location ~ \.php$ {
        fastcgi_split_path_info ^(.+\.php)(/.+)$;
        fastcgi_pass php-fpm:9000;
        fastcgi_index index.php;

        fastcgi_param SCRIPT_FILENAME $document_root/$fastcgi_script_name;

        include fastcgi_params;
    }
}

server {
    listen 80;
    sendfile off;

    server_name apipaste.cloudykingdom.com;

    root /web/mclogs/api/public;

    error_log /var/log/nginx/api-mclogs-error.log error;

    client_max_body_size 210m;

    location / {
        try_files $uri /index.php;
    }

    location ~ \.php$ {
        fastcgi_split_path_info ^(.+\.php)(/.+)$;
        fastcgi_pass php-fpm:9000;
        fastcgi_index index.php;

        fastcgi_param SCRIPT_FILENAME $document_root/$fastcgi_script_name;

        include fastcgi_params;
    }
}

server {
    listen 80;
    server_name _;  # Catch-all for debugging
    sendfile off;

    location / {
        return 200 "Host: $host\nServer Name: $server_name\nHttp Host: $http_host\n";
        add_header Content-Type text/plain;
    }
}
